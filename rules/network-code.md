# 网络代码规则 (Network Code Rules)

- 服务器权威：所有有意义的游戏状态变更（伤害、位置、物品）必须由服务器验证
- 版本化消息：所有网络消息必须包含版本号以支持未来兼容
- 安全性：所有客户端输入必须经过服务器验证。绝不相信客户端发送的数据
- 带宽管理：优先传输对游戏性关键的状态。对不重要的数据进行限流
- 延迟补偿：为关键战斗交互实现延迟补偿
- 状态同步：仅同步必要的状态。使用相关性过滤减少不必要的数据传输
- 重连处理：网络中断后必须能恢复到一致状态
