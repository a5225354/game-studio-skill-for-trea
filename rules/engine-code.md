# 引擎代码规则 (Engine Code Rules)

- 零分配热路径：核心更新循环中不得不必要的内存分配
- 线程安全：从多个线程访问的所有共享状态必须受锁或原子操作保护
- API 稳定性：公共 API 签名不得频繁变更。已弃用的 API 必须标记为 deprecated 并提供迁移路径
- 资源管理：所有原生资源必须显式释放，无悬空指针
- 平台抽象：平台特定代码必须封装在抽象层后
- 崩溃安全：所有资源分配路径必须有失败回退
