# UI 代码规则 (UI Code Rules)

- 不持有游戏状态所有权：UI 仅展示游戏状态，通过事件/信号请求操作。绝不直接修改游戏数据
- 本地化就绪：所有面向用户的文本必须通过本地化系统 — 禁止硬编码字符串
- 无障碍访问：支持文字缩放、色盲模式、键盘导航和手柄导航
- 输入无关：UI 逻辑不应区分键盘/鼠标和手柄输入 — 使用统一的动作抽象
- 数据绑定：使用数据绑定而非手动更新。UI 应反映底层状态的变化
- 分辨率无关：UI 布局应适应不同分辨率和宽高比
- 动画可跳过：所有 UI 动画应尊重"减少动态效果"设置或可以被跳过
